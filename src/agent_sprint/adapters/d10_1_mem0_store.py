"""Day 10 · Mem0 behind your MemoryStore interface.

WHAT IT MUST DO
    Implement memory/d10_1_store.py's interface with Mem0, changing nothing about
    how the harness calls it.

TO BUILD
    [ ] Map your add / search / consolidate / forget onto Mem0's model.
        Where it does not map, that mismatch is the finding -- note it.
    [ ] Run the same two tests: the temporal query, and PDPA deletion.
    [ ] Prove deletion actually deleted, in their store, not just in your
        index.

TOOL VERDICT (journal)
    Five questions, plus: read the Mem0 benchmark paper critically. Vendor
    benchmarks are contested.
"""
