"""Day 12 · The same flow, free-running on your own harness.

WHAT IT MUST DO
    Solve the dispute task with tools and a goal, and nothing else
    prescribed.

TO BUILD
    [ ] Same tools, same policy corpus, same approval gate as the graph.
    [ ] The flow lives in the prompt as a goal and constraints, not as
        nodes.
    [ ] Same limits, same tracing, same eval harness.
    [ ] Change exactly one variable between this and d12_1_dispute_graph.py:
        who decides the order of operations.

DONE WHEN
    Both run the Day 11 suite, and the difference in the numbers is
    attributable to that one variable.
"""
