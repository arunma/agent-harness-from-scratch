"""Day 6 · recall@k and MRR over the golden set. About 30 lines.

WHAT IT MUST DO
    Score retrieval on its own, separately from generation, so you can tell
    a retrieval failure from a hallucination.

TO BUILD
    [ ] Read the golden set from settings.golden_dir: question -> the chunk
        ids that answer it. You write these 30 by hand; that is the point.
    [ ] recall@k for k in (1, 5, 10, 20, 50).
    [ ] MRR over the whole set.
    [ ] Per-question output, not just the mean. The mean hides the misses
        you need to read.
    [ ] A table renderer, since the shoot-out runs this across several
        retrieval configurations.

DONE WHEN
    The embedding shoot-out table is generated by this file, and you have
    examined every miss and labelled its cause: parse, chunk boundary,
    vocabulary mismatch, or a bad question.
"""
