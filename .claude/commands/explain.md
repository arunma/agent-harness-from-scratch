---
description: Concept-first explanation with a worked numerical example
argument-hint: <concept, e.g. "prompt caching" or "MCP capability negotiation">
---
Explain: $ARGUMENTS

Structure:
1. **The idea** in plain words (3–5 sentences).
2. **A worked example with real numbers** — tokens, bytes, scores, latencies, whatever applies.
3. **Where it shows up in code** — which library call, protocol message or harness component; tag `[SDK]`, `[H]`, `[P]` or `[T]`.
4. **nanochat bridge**, only if a genuine one exists.
5. **One check question** for Arun to answer.

If a diagram helps, use Mermaid with `<br/>` (not `\n`) for line breaks. Don't write learn-zone implementation code.
